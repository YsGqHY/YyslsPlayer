import type { Messages } from '@/i18n';

// HomePage English copy. Mounted under the `home.*` namespace.
export const homePageEnUS: Messages = {
  home: {
    eyebrow: '燕云流音 · Where Winds Meet 36-Key Mode',
    hero: '燕云流音 MIDI performance tool',
    subtitle: '燕云流音 imports MIDI files, adjusts pitch ranges, key mapping and playback speed, then previews or performs through key simulation. The project targets 36-key mode only.',
    footer: {
      windowsOnly: 'Windows-only performance path: MIDI parsing, PlayPlan scheduling and SendInput key simulation.',
      scope: 'This version targets Where Winds Meet 36-key mode only.',
    },
  },
};
